---
import { SignIn } from "auth-astro/components";

import { css } from "@/styled-system/css";
---

<SignIn
  provider="github"
  class={css({
    _hover: {
      bg: "gray.600",
    },
    bg: "gray.700",
    borderRadius: "md",
    color: "white",
    h: "max-content",
    p: 2,
    w: "max-content",
  })}
>
  <slot />
</SignIn>
