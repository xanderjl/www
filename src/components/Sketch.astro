---
import { css } from "@/styled-system/css";
---

<div
  class={css({
    flex: 1,
    display: "flex",
    justifyContent: "center",
    alignItems: "center",
  })}
>
  <div
    id="container"
    class={css({
      display: "flex",
      boxShadow: "1px 3px 6px -1px rgba(0, 0, 0, 0.5)",
    })}
  >
  </div>
</div>
