import { defineTextStyles } from "@pandacss/dev";

export const textStyles = defineTextStyles({
  body: {
    value: {
      // fontFamily: "body",
      // fontWeight: "normal",
      // fontSize: "md",
      // lineHeight: 1.5,
      // letterSpacing: "0",
      // textDecoration: "none",
      // textTransform: "none",
    },
  },
});
