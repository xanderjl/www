import { defineTextStyles } from "@pandacss/dev";

export const textStyles = defineTextStyles({
  body: {
    value: {
      fontFamily: "body",
      fontSize: "md",
      fontWeight: "normal",
      letterSpacing: "0",
      lineHeight: 1.5,
      textDecoration: "none",
      textTransform: "none",
    },
  },
});
